name: lab08-py39
channels:
  - conda-forge
dependencies:
  - python=3.9
  # Use a numpy version compatible with SciPy (avoid numpy 2.x in this env)
  - numpy=1.26
  - scipy
  # Install OpenCV from conda-forge (includes contrib features more reliably)
  - opencv
  - pip
  - pip:
    # If you need additional pip-only packages, list them here (do NOT include numpy or opencv)
    # - some-pip-only-package>=1.0
